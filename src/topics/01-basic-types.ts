let text: string = 'texto de prueba';

const textoFijo: string = 'Este texto no es modificable';

const isActive: boolean = true;

let hpPoints: number | 'FULL' = 95;

console.log({
    text,
    textoFijo,
    isActive,
    hpPoints
})
export {
    
}